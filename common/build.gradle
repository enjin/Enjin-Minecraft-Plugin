sourceCompatibility = 1.7

dependencies {
    compile project(path: ':rpcapi', configuration: 'shadow')
    provided project(':core')
}

shadowJar {
    dependencies {
        include(project(':rpcapi'))
    }
}